import { DEFAULT_BASIC_LIST_SETTING } from "./setting/BasicListSetting";

const {
  pageSizeOptions,
  pageSize,
  size,
} = DEFAULT_BASIC_LIST_SETTING;

// Optional display number per page;
export const LIST_PAGE_SIZE_OPTIONS = pageSizeOptions;

// Number of items displayed per page
export const LIST_PAGE_SIZE = pageSize;

// Default Size
export const LIST_DEFAULT_SIZE = size;

//  Default layout of table cells
export const LIST_DEFAULT_ALIGN = 'center';
